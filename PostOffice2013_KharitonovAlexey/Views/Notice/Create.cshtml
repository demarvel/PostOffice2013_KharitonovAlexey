@model PostOffice2013_KharitonovAlexey.Models.Notice

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Notice</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.DateOfNotification)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.DateOfNotification)
            @Html.ValidationMessageFor(model => model.DateOfNotification)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.SecondNotice)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.SecondNotice)
            @Html.ValidationMessageFor(model => model.SecondNotice)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
