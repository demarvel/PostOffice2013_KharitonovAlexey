@model PostOffice2013_KharitonovAlexey.Models.Secogramma

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Secogramma</legend>

        @Html.HiddenFor(model => model.ID)

        <div class="editor-label">
            @Html.LabelFor(model => model.IDPostItem)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.IDPostItem)
            @Html.ValidationMessageFor(model => model.IDPostItem)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Registered)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Registered)
            @Html.ValidationMessageFor(model => model.Registered)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
